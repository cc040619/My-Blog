

# 评论相关接口开发文档
## 2.11 文章评论列表接口
### 2.11.1 需求
文章详情页面需要展示当前文章下的评论列表。
排序规则：
- 根评论（一级评论）：按创建时间**降序**
- 子评论（回复）：按创建时间**升序**

### 2.11.2 接口设计
#### 请求信息
| 请求方式 | 请求地址               |
| -------- | ---------------------- |
| GET      | `/comment/commentList` |

- 请求头：无需 Token
- 请求参数（Query 形式）：

| 参数名    | 说明     |
| --------- | -------- |
| articleId | 文章id   |
| pageNum   | 页码     |
| pageSize  | 每页条数 |

#### 响应格式
```json
{
  "code": 200,
  "data": {
    "rows": [
      {
        "children": [
          {
            "content": "学到很多",
            "createBy": "3",
            "createTime": "2023-05-16 12:08:05",
            "id": "3",
            "toCommentUserId": "1",
            "toCommentUserName": "admin",
            "username": "小胖"
          },
          {
            "content": "👍",
            "createBy": "1",
            "createTime": "2023-05-16 12:09:02",
            "id": "5",
            "toCommentUserId": "3",
            "toCommentUserName": "小胖",
            "username": "admin"
          }
        ],
        "content": "这篇文章写得真好",
        "createBy": "1",
        "createTime": "2023-05-16 12:06:38",
        "id": "2",
        "toCommentUserId": "-1",
        "username": "admin"
      }
    ],
    "total": "1"
  },
  "msg": "操作成功"
}
```

### 2.11.3 VO对象与工具类准备
#### 1. 评论VO：CommentVo
```java
@Data
public class CommentVo {
    private Long id;
    private String content;
    private Long toCommentUserId;
    private String toCommentUserName;
    private Long createBy;
    private String username;
    private LocalDateTime createTime;
    private List<CommentVo> children;
}
```

#### 2. 属性拷贝工具类：BeanCopyUtils
封装对象、集合属性拷贝逻辑
```java
public class BeanCopyUtils {
    private BeanCopyUtils() {}

    public static <V> V copyBean(Object source, Class<V> clazz) {
        if (source == null) {
            return null;
        }
        try {
            V result = clazz.getDeclaredConstructor().newInstance();
            BeanUtils.copyProperties(source, result);
            return result;
        } catch (Exception e) {
            throw new RuntimeException("Bean copy failed: " + clazz.getName(), e);
        }
    }

    public static <O, V> List<V> copyBeanList(List<O> list, Class<V> clazz) {
        if (list == null || list.isEmpty()) {
            return Collections.emptyList();
        }
        return list.stream()
                .map(o -> copyBean(o, clazz))
                .collect(Collectors.toList());
    }
}
```

### 2.11.4 代码实现
#### ① 展示根评论
##### CommentController
```java
@RestController
@RequestMapping("/comment")
public class CommentController {
    @Autowired
    private ICommentService commentService;

    @GetMapping("/commentList")
    public ResponseResult commentList(Long articleId, Integer pageNum, Integer pageSize) {
        return commentService.commentList(articleId, pageNum, pageSize);
    }
}
```

##### CommentService 接口
```java
public interface ICommentService extends IService<Comment> {
    ResponseResult commentList(Long articleId, Integer pageNum, Integer pageSize);
}
```

##### CommentServiceImpl 实现类
```java
@Service
public class CommentServiceImpl extends ServiceImpl<CommentMapper, Comment> implements ICommentService {
    @Autowired
    private CommentMapper commentMapper;
    @Autowired
    private UserMapper userMapper;

    @Override
    public ResponseResult commentList(Long articleId, Integer pageNum, Integer pageSize) {
        // 构造查询条件，查询指定文章根评论
        // 分页查询
        // 将Comment列表封装为commentVo列表
    }

    // 构造commentVo列表
    private List<CommentVo> buildCommentVoList(List<Comment> commentlist) {
        List<CommentVo> commentVoList = BeanCopyUtils.copyBeanList(commentlist, CommentVo.class);
        // 查询用户昵称并赋值
        return commentVoList;
    }
}
```

#### ② 展示子评论
##### CommentServiceImpl 补充逻辑
```java
@Override
public ResponseResult commentList(Long articleId, Integer pageNum, Integer pageSize) {
    // 构造查询条件，查询指定文章根评论
    // 分页查询
    // 将Comment列表重新封装为commentVo列表
    // 查询所有根评论对应的子评论，并且赋值给children属性
}

// 根据根评论id查询对应的子评论集合
private List<CommentVo> getChildComments(Long id) {
    // 子评论查询逻辑
}
```

---

## 2.12 友链评论列表接口
### 2.12.1 需求
友链页面展示对应评论列表，复用评论相关逻辑，区分**文章评论**与**友链评论**。

### 2.12.2 接口设计
#### 请求信息
| 请求方式 | 请求地址                   |
| -------- | -------------------------- |
| GET      | `/comment/linkCommentList` |

- 请求头：无需 Token
- 请求参数（Query 形式）：

| 参数名   | 说明     |
| -------- | -------- |
| pageNum  | 页码     |
| pageSize | 每页条数 |

#### 响应格式
```json
{
  "code": 200,
  "data": {
    "rows": [
      {
        "children": [
          {
            "content": "网站做得很漂亮，想申请一个友链",
            "createBy": "3",
            "createTime": "2023-05-16 12:08:21",
            "id": "4",
            "toCommentUserId": "1",
            "toCommentUserName": "admin",
            "username": "小胖"
          },
          {
            "content": "👌",
            "createBy": "1",
            "createTime": "2023-05-16 12:09:18",
            "id": "6",
            "toCommentUserId": "3",
            "toCommentUserName": "小胖",
            "username": "admin"
          }
        ],
        "content": "申请友链请留下网站名称和地址🎉🎉",
        "createBy": "1",
        "createTime": "2023-05-16 12:05:33",
        "id": "1",
        "toCommentUserId": "-1",
        "username": "admin"
      }
    ],
    "total": "1"
  },
  "msg": "操作成功"
}
```

### 2.12.3 代码实现
#### 1. 系统常量类 SystemConstants
区分评论类型
```java
/**
 * 评论类型为：文章评论
 */
public static final String ARTICLE_COMMENT = "0";
/**
 * 评论类型为：友链评论
 */
public static final String LINK_COMMENT = "1";
```

#### 2. CommentController 改造
新增接口，传入评论类型参数
```java
@RestController
@RequestMapping("/comment")
public class CommentController {
    @Autowired
    private ICommentService commentService;

    @GetMapping("/commentList")
    public ResponseResult commentList(Long articleId, Integer pageNum, Integer pageSize) {
        return commentService.commentList(
                SystemConstants.ARTICLE_COMMENT,
                articleId,
                pageNum,
                pageSize
        );
    }

    @GetMapping("/linkCommentList")
    public ResponseResult linkCommentList(Integer pageNum, Integer pageSize) {
        return commentService.commentList(
                SystemConstants.LINK_COMMENT,
                null,
                pageNum,
                pageSize
        );
    }
}
```

#### 3. CommentService 接口改造
新增 `commentType` 参数
```java
public interface ICommentService extends IService<Comment> {
    ResponseResult commentList(String commentType, Long articleId, Integer pageNum, Integer pageSize);
}
```

#### 4. CommentServiceImpl 实现类改造
接收评论类型，补充查询条件、非空判断
```java
@Service
public class CommentServiceImpl extends ServiceImpl<CommentMapper, Comment> implements ICommentService {
    @Override
    public ResponseResult commentList(String commentType, Long articleId, Integer pageNum, Integer pageSize) {
        // 改造 queryWrapper 查询条件，增加评论类型
        // 对 articleId 做非空判断
    }
}
```

